{
    "name": "@ortsarchiv-gemeinlebarn/volksschule-neubau",
    "version": "1",
    "description": "Web Project",
    "browserslist": "> 0.5%, last 2 versions, not dead",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "parcel": "npx parcel",
        "start": "parcel src/index.html --no-cache --open",
        "start:landing": "parcel src/landing/index.html",
        "start:audio": "parcel src/audio/index.html",
        "ci:build": "parcel build src/index.html --log-level error --no-cache --dist-dir ./dist/main",
        "ci:build:landing": "parcel build src/landing/index.html --log-level error --no-cache --dist-dir ./dist/landing",
        "ci:build:audio": "parcel build src/audio/index.html --log-level error --no-cache --dist-dir ./dist/audio --public-url /audio/"
    },
    "author": "Jakob Vesely",
    "license": "ISC",
    "dependencies": {
        "@ortsarchiv-gemeinlebarn/components": "^2.21.0"
    },
    "devDependencies": {
        "@parcel/transformer-sass": "^2.16.0",
        "buffer": "^6.0.3",
        "parcel": "^2.16.0"
    }
}
